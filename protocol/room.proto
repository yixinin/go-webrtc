syntax = "proto3";
package protocol; 



//断开连接
message KickPeerReq{

}

message KickPeerAck{

}


//如果音视频都不接收则断开连接 下次开启时再连接
message ControlReq{
    int64 uid = 1; 
    int64 fromUid = 2;
    bool videoOn = 3;
    bool audioOn = 4;
}

message ControlAck{
    bool disconnected = 1; //是否断开连接
}